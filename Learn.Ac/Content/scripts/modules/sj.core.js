/**
 * 生成sj顶层对象，js插件都扩展自sj对象
 */
var sj = (function () {
    'use strict'
    var sj = sj || {};
    return sj;
}());

//页面初始化
(function ($) {

    //页面加载完成初始化
    $(document).ready(function () {
         

    });
}(jQuery))